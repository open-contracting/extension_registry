language: python
cache: pip
python:
  - "3.6"
install:
  - pip install -r requirements.txt
  - pip install pytest
script:
  - PYTHONPATH=$PYTHONPATH:.  pytest --pyargs ocdsextensionregistry.tests
  - python validate.py
